import React from 'react';
import PropTypes from 'prop-types';
import styled from "styled-components";

/**
 *
 * Hops
 *
 */
const Hops = (props) => {
  return (
    <div>
      Hops
    </div>
  );
};

Hops.propTypes = {};

export default Hops;
