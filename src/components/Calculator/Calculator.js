import React from "react";
import PropTypes from "prop-types";
import { css } from "@emotion/core";

/**
 *
 * Calculator
 *
 */
const Calculator = props => {
  return <div css={styles}>Calculator</div>;
};

Calculator.propTypes = {};

const styles = css`
  margin-right: 10px;
`;
export default Calculator;
